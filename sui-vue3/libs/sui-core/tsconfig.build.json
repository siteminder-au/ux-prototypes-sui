{
  "compilerOptions": {
    // we use vite-plugin-dts to generate .d.ts files instead
    "declaration": false,
    "incremental": true,
    "tsBuildInfoFile": "./.tsbuildinfo",
    // we want our compiled js code to use import/export instead of require/module.exports
    // 'ES2015' is used as rollup v4 supports ES2015, ES2020 or ESNext
    "module": "ES2015",
    "resolveJsonModule": true,
    // NOTE: "allowSyntheticDefaultImports" is automatically set to true when moduleResolution is set to `bundler`
    // see: https://www.typescriptlang.org/tsconfig#allowSyntheticDefaultImports
    // moduleResolution `bundler` is needed by rollup 4. see: https://vitejs.dev/guide/migration#rollup-4
    "moduleResolution": "bundler",
    // we set target: "ES2022" to remove issue:
    // A comment "/* @__PURE__ */" in <FILE> contains an annotation that Rollup cannot interpret due to the position of the comment.
    // The comment will be removed to avoid issues.
    // NOTE: ensure that `target` in tsconfig.build.json has the same value as `build.target` in vite.config.ts file for consistency purposes.
    "target": "ES2022",
    "outDir": "dist",
    "rootDir": "src/app",
    "baseUrl": ".",
    "lib": ["es5", "es6", "dom", "es2016", "es2017"],
    // some eslint rules such as
    // - eslint@typescript-eslint/no-unnecessary-condition
    // - @typescript-eslint/prefer-nullish-coalescing
    // requires "strict" set to true
    // see: https://github.com/standard/eslint-config-standard-with-typescript/issues/481
    "strict": true,
  },
  "exclude": [
    "**/*.spec.ts"
  ],
  "include": [
    "./src/app/*"
  ],
  "compileOnSave": false
}
